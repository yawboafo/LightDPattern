# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MobileMoneyPayment' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!


    pod 'Disk', '~> 0.3.3'
    pod 'SnapKit', '~> 4.0.0'
    pod 'SwiftyJSON'


  # Pods for MobileMoneyPayment

  target 'MobileMoneyPaymentTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
